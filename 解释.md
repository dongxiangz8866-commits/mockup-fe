# 服装 Mockup 技术调研 · 通俗解释版

> 配套 [`RESEARCH.md`](./RESEARCH.md) 阅读。RESEARCH 写的是结论和选型，本文解释里面那些"看不懂的名词"，并把每个方案的"原理"用大白话讲清楚。
> 阅读顺序建议：先看 §1 名词表，再看 §2 技术路线（按 RESEARCH 顺序），看完后回到 RESEARCH 决策矩阵就能秒懂。

---

## 1. 名词表（先扫一眼）

### 1.1 图像基础

| 名词 | 大白话解释 | 类比 |
|------|---------|------|
| **像素 / pixel** | 图片最小的方块 | 马赛克的一个格子 |
| **alpha 通道** | 控制"透明度"的那一层数据 | PNG 抠图后的"透明背景"就是 alpha |
| **RGB / RGBA** | R 红 G 绿 B 蓝（+A 透明），一张彩色图就是这四张灰度图叠在一起 | 印刷的 CMYK 类似 |
| **UV** | 给一张贴图建立的坐标系，范围都是 0~1 | 像在地图上找点的"经纬度" |
| **贴图 / texture** | 任何能"贴到"几何体表面的图 | 给 3D 模型穿衣服 |
| **shader** | 一段在显卡上跑的小程序，决定每个像素最终长什么样 | "颜色加工厂"的流水线 |
| **GLSL** | 写 shader 的语言 | shader 界的"JavaScript" |

### 1.2 把图案"贴到"衣服上要解决的事

| 名词 | 大白话 |
|------|------|
| **mask（蒙版）** | 一张黑白图，**白色区域**就是"可以印东西的地方"，黑色就是"不能碰"。比如把 T 恤的胸口涂白，其他地方涂黑 |
| **displacement map（置换贴图）** | 一张特殊的图，每个像素的颜色 = 图案要被推动多少。颜色越亮推得越远，**用来让平面图案贴合褶皱** |
| **normal map（法线贴图）** | 用 RGB 三个颜色编码"这个点的表面朝哪个方向"，让平面看起来有凹凸 | 假装的 3D 凹凸 |
| **depth map（深度图）** | 一张灰度图，越亮 = 离镜头越近 | "距离热力图" |
| **light map（光影贴图）** | 一张灰度图，记录"原图哪里亮哪里暗"。把它和图案相乘，图案就有了光影 |
| **segmentation（分割）** | 让 AI 自动告诉你"这张图哪部分是衣服"，输出就是个 mask |
| **pose / landmark（姿态关键点）** | AI 检测出"左肩在 (320, 150)"等几十个关节点 | 火柴人骨架 |

### 1.3 变形 / 渲染

| 名词 | 大白话 |
|------|------|
| **mesh（网格）** | 用很多三角形拼出来的"形状骨架" | 折纸做的鸽子 |
| **UV warp / mesh warp** | 移动 mesh 上的点 → 让贴在上面的图也跟着扭曲 | 把橡皮膜捏一下，上面的图就变形了 |
| **perspective transform（透视变换）** | 把矩形拉成不规则四边形，模拟"远处看起来小" | PPT 里图片的"3D 旋转" |
| **TPS (thin-plate-spline)** | 一种**平滑**的变形算法，给几个控制点，剩下的地方它帮你顺滑过渡 | 拉橡皮泥 |
| **decal** | "贴花"——在 3D 模型表面贴一张图，像汽车上的车贴 |
| **PBR lighting** | Physically Based Rendering，按真实物理算光的渲染方式 | 让 3D 看起来像照片 |
| **HDRI** | 高动态范围的环境照，用来给 3D 场景"打灯" | 把 3D 模型放进真实环境的"虚拟摄影棚" |

### 1.4 混合模式（Photoshop 里的那些）

| 名词 | 公式（直观） | 效果 |
|------|------|------|
| **alpha 叠加** | 直接覆盖 | 像贴纸 |
| **multiply（正片叠底）** | A × B | **保留底图的暗部**，常用来让图案带上衣服的褶皱阴影 |
| **overlay（叠加）** | 亮的更亮、暗的更暗 | 既保留图案颜色又保留光影 |

> ✨ 关键直觉：**衣服褶皱 = 阴影 = 图案要变暗**。所以"图案 × 光影图"= 真实印花效果。

### 1.5 AI 模型相关

| 名词 | 大白话 |
|------|------|
| **Stable Diffusion (SD/SDXL/SD3)** | 当前最流行的开源"文生图"AI 模型 |
| **Inpainting（重绘）** | 你画一个 mask，AI 只在 mask 区域重新生成 | 图片版的"局部 PS" |
| **ControlNet** | SD 的"控制器"，给它一张姿势图/线稿/深度图，让生成的结果**保持构图** | 给 AI 戴上指挥棒 |
| **IP-Adapter** | 把"参考图"喂给 SD，让生成结果**风格/内容像参考图** | 给 AI 看样子说"照着这个画" |
| **VTON (Virtual Try-On)** | 专门为"换衣服"训练的 AI 模型 |
| **SMPL / SMPL-X** | 学术界一个**人体三维模型**的参数化表示，几十个数字就能描述一个人的身材+姿势 | 人体的"乐高蓝图" |
| **MediaPipe** | Google 出的**前端能跑**的轻量 AI 模型套装，能检测姿态/手/脸 |
| **ONNX Runtime Web** | 让 PyTorch/TensorFlow 训练好的 AI 模型**在浏览器里跑**的运行时 |

### 1.6 工程栈

| 名词 | 是什么 |
|------|------|
| **pixi.js** | 专做 2D 的 WebGL 渲染库，性能比 Canvas2D 好得多 |
| **three.js** | 浏览器里最流行的 3D 渲染库 |
| **react-three-fiber (R3F)** | 用 React 写 three.js 的语法糖 |
| **drei** | three.js 的"工具箱"（含 Decal、Environment 等高频组件） |
| **DisplacementFilter** | pixi.js 内置的滤镜，给一张 displacement 图就能让画面扭曲 |
| **PSD smart object** | Photoshop 里的"智能对象"——一种可替换内容的图层。Mockup 模板就是它做的 |
| **POD (Print on Demand)** | 按需印刷电商模式：用户下单后才印，不囤货 |

---

## 2. 把每个方案讲明白

### 2.1 子问题为什么是 5 个？

要把一张平面图案"印"上衣服，本质上要解决：
1. **图案要贴在哪？**（mask）
2. **衣服表面是什么形状？**（褶皱起伏）
3. **图案怎么跟着形状扭？**（变形）
4. **图案要不要变暗变亮？**（光影）
5. **用户能不能拖拽缩放？**（交互）

**所有方案的差别都在 ① 和 ②**——你是手动给的、还是 AI 算的、还是 3D 模型自带的。
③④⑤ 大家都差不多。

---

### 2.2 方案 A · 纯透明叠加 ★

**做法**：把图案 PNG 用 CSS `position: absolute` 摞在模特图上面，完事。

**问题**：图案是平的，衣服是有褶皱有透视的——一眼假，像贴纸。

**适用**：卡通风、不在乎真实感的场景。

---

### 2.3 方案 B · 2D Mesh Warp（手动锚点）★★

**做法**：在衣服上手动点 4~9 个控制点（左肩、右肩、左下、右下…），把图案按这些点做"梯形拉伸"。

**类比**：PPT 里把一张图的四个角拖到不规则四边形里。

**问题**：解决了透视，但没褶皱、没光影。

---

### 2.4 方案 C · 2D Displacement + Light（PSD 工作流）★★★★ ⭐推荐

**这个是 Printful、Placeit、Smartmockups 全行业都在用的标准做法。**

**核心思路**：把"褶皱"和"光影"信息**事先**烘焙成两张贴图，运行时只是把图案当贴图替换一下。

#### 资产准备（美工一次性做）

| 文件 | 内容 | 怎么做 |
|------|------|------|
| `base.jpg` | 模特原图 | 摄影 |
| `mask.png` | 衣服可印区域（黑白） | PS 抠图 |
| `displace.png` | "推哪去"的位移图 | PS 滤镜→扭曲→置换 用的灰度图 |
| `light.png` | 光影灰度图 | base 转灰度 + 调级 |

#### 运行时怎么算

伪代码（每个像素都跑一遍）：

```
对屏幕上的每个像素 (x, y)：
  1. 查 displace 图：在这个位置图案要被推到 (x', y')
  2. 从图案上取 (x', y') 的颜色 = graphic
  3. 查 light 图：这个位置该多亮 = light
  4. graphic × light = 带光影的图案
  5. mask 决定能不能盖：mask 白的地方覆盖 base，黑的地方保留 base
```

> 💡 整个过程没有 AI、没有 3D，**只有查表和乘法**，所以非常快，浏览器 60 帧轻松。

#### 优缺点
- ✅ 真实感 ★★★★（行业标准）
- ✅ 实时帧率高，可做视频/GIF
- ✅ 纯前端，pixi.js 内置 `DisplacementFilter` 现成
- ❌ 只能用**预先做好的模板**，用户上传的任意照片用不了
- ❌ 每个模板美工 30 分钟到 1 小时

---

### 2.5 方案 D · 2D + AI 分割 + 自动 Displacement ★★★

**思路**：让 AI 把"美工活"自动化——`mask` 用衣服分割模型自动生成，`displace` 用深度估计模型自动生成。

**问题**：
- 模型几十到几百 MB，首次加载慢
- AI 出的 displacement **远不如手画**精细，褶皱处会"贴纸感"
- 折中：让模型在后端跑，前端只合成

---

### 2.6 方案 E · 全 3D 渲染 ★★★

**思路**：场景里全是 3D 的——3D 模特 + 3D T 恤，把图案当成 T 恤的"贴花（decal）"。

**类比**：游戏里给角色换皮肤。

- ✅ 视角姿势完全可控，可以转着看
- ❌ 失去真实摄影感（不像照片，像 CG）
- ❌ 3D 衣服和模特资产成本高

---

### 2.7 方案 F · 3D Plane + Pose 估计 ★★★ ⭐任意照片折中方案

**思路**：保留真实照片当背景，**只**在前面摆一块朝向相机的 3D 平面，把图案贴在上面，用 AI 姿态识别把这块平面对准胸部。

**步骤**：
1. MediaPipe 检测：得到肩、髋等几十个 2D 点
2. 用左肩、右肩、左髋、右髋四个点圈出"胸部区域"
3. three.js 在那个位置放一个四边形
4. 把图案贴上去，加点 procedural（程序化生成的）噪声当假褶皱

**优缺点**：
- ✅ 任意照片可用，纯前端
- ✅ MediaPipe 模型才 ~10MB
- ❌ 褶皱靠假的程序化生成，不真实
- ❌ 极端姿势（侧身、低头）容易识别失败

---

### 2.8 方案 G · SMPL 体型重建 ★★★★

**思路**：从 2D 照片**反推**人的 3D 身材+姿势（SMPL 参数），把 3D 衣服"穿"到这个 3D 人身上做布料模拟。

**这是论文级方案**——TryOnDiffusion、StableVITON 都走这条路。

**问题**：必须 GPU + 训练好的网络，前端不可能。商业系统（Zalando、阿里）都是后端。

---

### 2.9 方案 H · Stable Diffusion + ControlNet + IP-Adapter ★★★★★ ⭐效果天花板

**这是现在 AI 圈最通用的"换印花"方法。**

**工作流（ComfyUI 节点图）**：

```
模特图 ──┬─► SAM 自动选中衣服区 ──► 得到 mask
        └─► ControlNet 提取姿势 + 边缘 ──┐
                                       ├─► SDXL 在 mask 区域重画 ──► 输出
图案图 ──► IP-Adapter 当参考 ─────────┘
```

逐个解释：

- **SAM**：Meta 出的"啥都能分割"模型，自动找出衣服轮廓
- **ControlNet**：拿姿势图 + 边缘图当"指挥棒"，确保 AI 重画时**不改模特姿势**
- **IP-Adapter**：把图案当参考喂进去，让生成的印花长得像
- **SDXL Inpaint**：在 mask 区域重新生成

**优缺点**：
- ✅ 效果最自然——褶皱、光影、阴影 AI 全帮你处理
- ✅ 任意照片 + 任意图案
- ❌ **必须 GPU**（3090 一张 5-15s）
- ❌ 文字、logo 容易扭曲
- ❌ 有随机性，常需"重新生成"

**部署**：
- 商业 API：Replicate、Fal.ai 按张计费 $0.005~0.05
- 自托管：自己开 GPU 跑 ComfyUI

---

### 2.10 方案 I · 虚拟试穿（VTON）模型 ★★★★★

**为"换衣服"专门训练的模型**，和 H 路线的差别：H 喂的是"印花图"，I 喂的是"已经印好图案的整件衣服"。

代表：IDM-VTON、CatVTON、OOTDiffusion（字节）、阿里 OutfitAnyone。

**用在印花场景**：先用 H 路线 / PS 模板做出"平铺的印花衣服"，再喂给 VTON → 两阶段管线。

---

### 2.11 方案 J · 商业 Mockup API ★★★★ ⭐最快上线

**就是别人把方案 C（PSD displacement）做成了云 API**——你传图案，它返回带模特的成品图。

| 服务 | 价格 |
|------|------|
| DynamicMockups | $19+/月，可上传自己的 PSD |
| Printful Mockup | 免费（仅 Printful 商品） |
| Smartmockups | $9+/月 |

**优缺点**：
- ✅ 1-2 天上线 MVP
- ❌ 模板/模特受平台限制

---

### 2.12 方案 K1 · Recraft（AI 一体化 SaaS · 海外）★★★★

**和 J 的关键差别**：J 是"云端 PSD"（确定性），K 是"云端 AI"（生成式）。

**Recraft 是什么**：海外一家通用 AI 设计平台，文生图+矢量+mockup 都做。

**它的 mockup 怎么用**：
1. 上传图案
2. 选一个模板（T 恤、卫衣等）
3. AI 自动贴合，自带阴影、曲面、光影
4. 还有 "Convert any image to mockup" — 把任意图变 mockup

**API**：
- REST 调用，文档清晰
- 价格：raster 图 $0.04/张，mockup 操作 = 2 credits/次
- 免费每天 30 张

**适合**：自研产品想要"AI 真实感 mockup"，但**不想自己跑 GPU/SD**。

---

### 2.13 方案 K2 · 灵图POD（AI 一体化 SaaS · 国内跨境 POD）★★★★

**这个不是给开发者集成的，是给"卖家自己用的工作站"。**

**它做什么**：厦门灵图科技，专门给跨境 POD（按需印刷）卖家做的全流程工具：

```
采集别家爆款 → 抠图 → 印花裂变 → AI 套图（mockup）
            → 标题生成 → 一键上架 Amazon/Temu/Etsy/Shein/TikTok
            → 侵权检测
```

**Mockup 那一步叫"AI 套图"**：上传图案 → 选品类（T 恤/卫衣/毛毯）+ 选场景（咖啡店/家居）→ 30 秒出 5-8 张多角度模特实穿图。

**关键认知**：
- ✅ 国内跨境 POD 卖家"自己用"很方便
- ❌ **不是开发者 API**——你**不能**把它集成进自家产品

---

## 3. 三家"AI mockup"产品的差别（最容易混）

很多人会把 J / K1 / K2 搞混。一句话区分：

| 你是谁 | 用哪个 |
|------|------|
| 自研产品的开发者，要可控、可上传自己 PSD | **J. DynamicMockups / Printful API** |
| 自研产品的开发者，要"AI 真实感"且不想自维护 GPU | **K1. Recraft API** |
| 跨境 POD 卖家，自己开店铺，**不写代码** | **K2. 灵图POD 工作站** |

底层技术：
- J = 云端 PSD displacement（确定，没随机）
- K1 / K2 = 云端 SD AI（更真实，但有随机性）

---

## 4. 一张表：每个方案在 5 个子问题上的覆盖

| 方案 | ① 哪里 | ② 形状 | ③ 变形 | ④ 光影 | ⑤ 交互 |
|------|:---:|:---:|:---:|:---:|:---:|
| A. 纯叠加 | 手动 | ❌ | ❌ | ❌ | ✅ |
| B. Mesh Warp | 手动 | ❌ | ✅ | ❌ | ✅ |
| C. PSD Displacement | 美工烘焙 | 美工烘焙 | ✅ | 美工烘焙 | ✅ |
| D. AI 自动 Displacement | AI | AI | ✅ | 取自原图 | ✅ |
| E. 全 3D | UV | 3D 模型 | ✅ | PBR | ✅ |
| F. 3D Plane + Pose | Pose | 简单 | ✅ | 简单 | ✅ |
| G. SMPL 重建 | 3D 人体 | 3D 模型 | ✅ | PBR | ✅ |
| H. SD Inpaint | AI | AI | AI | AI | 部分 |
| I. VTON 模型 | AI | AI | AI | AI | ❌ |
| J. 商业 API | 平台预制 | 平台预制 | 平台预制 | 平台预制 | API |
| K1. Recraft | 平台 AI | 平台 AI | 平台 AI | 平台 AI | API |
| K2. 灵图POD | 平台 AI | 平台 AI | 平台 AI | 平台 AI | SaaS UI |

---

## 5. 怎么选（白话版）

### 你只关心一句话

| 你的情况 | 选 |
|------|------|
| 服装 SKU 固定 + 要纯前端 + 要美图 | **C（pixi.js + PSD）** |
| 用户上传任意照片 + 纯前端先跑起来 | **F（MediaPipe + 3D Plane）** |
| 要最高质量，能等 5-15 秒，可以接后端 | **H（SD + ControlNet）** |
| 1-2 天就要上线 | **J（DynamicMockups API）** |
| 想要 AI 真实感但不想自维护 GPU | **K1（Recraft API）** |
| 我是跨境卖家不是开发者 | **K2（灵图POD）** |

### 推荐组合（项目实施时）
1. **MVP 阶段**：方案 C，1-2 周，10-20 套 PSD 模板
2. **扩展阶段**：方案 F，覆盖任意照片
3. **高质量按钮**：方案 H 或 K1，按需调用

---

## 6. 名词速查（首字母排序）

- **alpha** — 透明度
- **ControlNet** — SD 的姿势/构图控制器
- **decal** — 贴花（3D 贴图）
- **displacement map** — 置换贴图（推动像素的图）
- **GLSL** — shader 语言
- **HDRI** — 环境光照贴图
- **inpainting** — 局部重绘
- **IP-Adapter** — SD 的"参考图"插件
- **landmark** — 关键点（如肩、髋）
- **light map** — 光影贴图
- **mask** — 蒙版（黑白图，控制可作用区域）
- **mesh** — 三角形网格
- **MediaPipe** — Google 的轻量前端 AI
- **multiply** — 正片叠底（A × B）
- **normal map** — 法线贴图（假凹凸）
- **ONNX Runtime Web** — 浏览器跑 AI 模型
- **overlay** — 叠加混合模式
- **PBR** — 真实物理光照
- **pixi.js** — 2D WebGL 库
- **POD** — 按需印刷
- **pose estimation** — 姿态识别
- **PSD smart object** — PS 智能对象
- **R3F (react-three-fiber)** — React 版 three.js
- **SAM** — Meta 分割一切模型
- **SD / SDXL** — Stable Diffusion 文生图
- **segmentation** — 图像分割
- **shader** — 显卡上的小程序
- **SMPL** — 参数化人体三维模型
- **texture** — 贴图
- **three.js** — 3D 库
- **TPS** — 平滑变形算法
- **UV** — 贴图坐标
- **VTON** — 虚拟试穿模型

---

## 7. 一句话总结

> **没有 AI 的方案靠"事先把褶皱光影画好"（C/J）；
> 有 AI 的方案靠"模型实时算出褶皱光影"（D/F/H/I/K）；
> 三维方案靠"真有一个 3D 形状自然产生褶皱光影"（E/G）。**
> 这就是所有路线的根本区别。
